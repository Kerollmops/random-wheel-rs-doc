initSidebarItems({"mod":[["random_wheel","A Little implementation of the random-wheel principle, `RandomWheel<T>`. ! ! # Examples ! ! You can explicitly create a `RandomWheel<T>` with `new()`: ! ! ``` ! let rw: RandomWheel<i32> = RandomWheel::new(); ! ``` ! ! You can `push` values onto the random-wheel (which will grow the wheel as needed): ! ! Popping values works in much the same way: ! ! ``` ! let one = 1; ! let two = 2; ! { !     let mut rw = RandomWheel::new(); ! !     rw.push(&one); !     rw.push(&two); ! !     let one_or_two = rw.pop(); ! } ! ```"]]});